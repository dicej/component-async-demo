[package]
name = "middleware"
version = "0.1.0"
edition = "2021"

[dependencies]
futures = "0.3.30"
once_cell = "1.19.0"
wit-bindgen = { git = "https://github.com/dicej/wit-bindgen", branch = "async" }
flate2 = "1.0.28"

[lib]
crate-type = ["cdylib"]
